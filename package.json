{
  "name": "lazyskill",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "skill": "./src/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build ./src/cli.ts --compile --outfile dist/skill",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/core": "^1.1.0",
    "@clack/prompts": "^1.1.0",
    "yaml": "^2.8.2"
  }
}
